package com.controllers;

/**
 * Created by alexey on 03.10.16.
 */
public class Util {

//    public static Map<DeviceEntity, List<AbstractTask>> generate(Monitor monitor) {
//        Map<DeviceEntity, List<AbstractTask>> map = new HashMap<>();
//        List<DeviceEntity> deviceList = monitor.getDeviceList();
//        List<Command> commands = monitor.getCommands();
//        for (DeviceEntity device : deviceList) {
//           map.putIfAbsent(device, new ArrayList<>());
//          //  map.get(device).addAll(putCriteria(device, ));
//        }
//     return null;
//    }

}
